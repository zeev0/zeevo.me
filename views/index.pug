extends header
block content
    body.home
        .site-container
            .site-header
                include navbar
                header
                    h1 zeevo.me
                    .tagline Shane
            article
                - var n = 0;
                while n < 5 && n < posts.length
                    - var post = posts[n];
                    .post-entry
                        .post-detail
                            h2
                                a(href='/posts/' + post.category + '/' + post.location) #{post.description}
                                span.post-date #{post.displayDate}
                            .post-summary #{post.summary}
                    - n += 1
                if posts && posts.length > 0
                    h2.readmore
                        a(href='posts') Read more posts »
